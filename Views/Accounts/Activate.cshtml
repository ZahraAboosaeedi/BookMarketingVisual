@model BookShop.Core.ViewModels.ActivateViewModel

@{
    ViewData["Title"] = "فعال سازی حساب کاربری";
    Layout = "~/Views/Shared/_Accountt.cshtml";
}

<h4 class="text-center">فعال سازی حساب</h4>
<hr />
<form asp-action="Activate">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <div class="form-group">
        <input asp-for="ActiveCode" class="form-control input-lg" placeholder="کد فعال سازی" />
        <span asp-validation-for="ActiveCode" class="text-danger"></span>
    </div>
    
    
    <div class="form-group">
        <input type="submit" value="فعال سازی حساب" class="btn-lg btn-block btn-submit" />
    </div>
    @*<div class="form-group">
        <h4 class="text-center">
            <a href="/">قبلا ثبت نام کرده اید</a>
        </h4>
    </div>*@
</form>